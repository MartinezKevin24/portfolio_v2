---
import "./styles.css";
import type { ProjectPreview } from "../../types/projects";
import { projectsPreviews } from "../../data/projectsPreviews";
import type { Lang } from "../../i18n/index";
import { TECH_STACK_PILL_CLASSES } from "../../constants/techStackPillClasses";
import ProjectPreviewCard from "../ProjectPreviewCard/index.astro";

const lang = (Astro.currentLocale ?? "es") as Lang;
---

<div class="grid grid-cols-1 gap-4 w-full">
  {
    projectsPreviews.map((project: ProjectPreview) => (
      <ProjectPreviewCard project={project} lang={lang} techClassMap={TECH_STACK_PILL_CLASSES} />
    ))
  }
</div>
